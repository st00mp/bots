# Copier vers .env (chmod 600) et remplir. Jamais de secret en dur dans le code.
KIMI_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Écran TRMNL (optionnel) : UUID webhook du plugin privé + clé MCP (gestion du markup)
TRMNL_PLUGIN_UUID=
TRMNL_MCP_KEY=
