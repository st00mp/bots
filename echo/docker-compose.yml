services:
  echo:
    build: .
    container_name: echo-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - GROQ_API_KEY=${GROQ_API_KEY}
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"
